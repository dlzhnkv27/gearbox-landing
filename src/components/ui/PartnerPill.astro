---
const { partner, ariaHidden = false } = Astro.props;
---

<span class="partner-pill">
  <span class="partner-logo">
    <img
      src={partner.src}
      alt={ariaHidden ? "" : `${partner.name} logo`}
      loading="lazy"
      decoding="async"
    />
  </span>
</span>
